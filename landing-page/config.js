// Firebase Analytics measurement ID for the landing page.
// Find it in Firebase Console → Project settings → Your apps → Web app → Measurement ID.
// Must match EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID in the app's .env file.
window.EASYREAD_ANALYTICS = {
  measurementId: 'G-XXXXXXXXXX',
};
